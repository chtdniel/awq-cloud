# Cloudflare & Wrangler
.wrangler/
.mf/

# Dependencies
node_modules/

# Local secrets (never commit — repo is PUBLIC)
.dev.vars

# Agent & IDE caches
.claude-flow/
.omo/
.impeccable/
.DS_Store
Thumbs.db

# Local QA artifacts (Playwright screenshots/traces) and scratch workspace.
# Repo is PUBLIC — these can contain production data captured during QA.
test-results/
scratch/

# Local working notes (may quote production data or credentials during debugging)
.debug-journal.md

# D1 database dumps (repo is PUBLIC; dumps contain password hashes + PII).
# Files are still exported locally to docs/backups/ — just never committed.
docs/backups/*.sql

# Office lock/temp files (e.g. public/~$briefing-template.xlsx while Excel has it open)
~$*

# Production database exports (contain operational data; never commit).
backups/
